# TikTok Live Connector Configuration
# Copy file ini ke .env dan isi dengan nilai yang sesuai

# Username TikTok (dengan @ atau tanpa)
TIKTOK_USERNAME=@anjayfollowers_

# Session ID untuk authenticated connection (opsional)
# Dapatkan dari browser cookies setelah login ke TikTok
TIKTOK_SESSION_ID=

# TT Target IDC untuk authenticated connection (opsional)
# Dapatkan dari browser cookies setelah login ke TikTok
TIKTOK_TT_TARGET_IDC=

# Port untuk webhook server (default: 3000)
PORT=3000

# Debug mode untuk melihat struktur data lengkap (set ke 'true' untuk enable)
DEBUG=false

# ============================================
# CATATAN: Feature Flags sudah dipindahkan ke config/config.json
# ============================================
# Untuk mengatur enable/disable fitur, edit file config/config.json
# Fitur tidak lagi dikontrol melalui environment variables

